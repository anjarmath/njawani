<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Njawani" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/orange</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/orange</item>
        <!-- Customize your theme here. -->
        <item name="fontFamily">@font/outfit_font</item>
    </style>

    <style name="fontaksara" parent="@android:style/TextAppearance.Small">
        <item name="fontFamily">@font/nykngayogyan</item>
    </style>

    <style name="SecondTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/orange</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/orange</item>
        <!-- Customize your theme here. -->
        <item name="fontFamily">@font/outfit_font</item>
    </style>

    <style name="MtButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="materialThemeOverlay">@style/ButtonThemeOverlay</item>
    </style>

    <style name="ButtonThemeOverlay">
        <item name="android:drawable">@drawable/gradien</item>
    </style>

    <style name="Buttonnya" parent="Widget.AppCompat.Button.Colored">
        <item name="android:background">@drawable/button_bg</item>
    </style>

</resources>